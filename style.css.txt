body {
    font-family: Arial, "Helvetica Neue", Helvetica, sans-serif;
    background-color: #f9f9f9; /* Light grey for a subtle background */
    width: 100%;
    margin: 0;
}

h1 {
    background-color: #c0392b; /* Dark red for the header */
    color: white;
    text-align: center;
    margin: 0;
    padding: 16px; /* Increased padding for better spacing */
    width: 100%;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2); /* Subtle shadow for depth */
}

nav {
    text-align: center;
    background-color: #27ae60; /* Green for the navigation bar */
    width: 100%;
    padding: 12px;
    margin: 0;
}

nav a {
    color: white; /* White text for links */
    text-decoration: none; /* Remove underline from links */
    padding: 8px 15px; /* Added padding for better clickable area */
    transition: background-color 0.3s; /* Smooth transition for hover effect */
}

nav a:hover {
    background-color: #1abc9c; /* Lighter green on hover */
}

section {
    text-align: center;
    padding: 20px; /* Added padding for sections */
}

@media (max-width: 600px) {
    body {
        background-color: #e8f4f8; /* Light blue for smaller screens */
    }

    h1 {
        font-size: 1.5em; /* Smaller font size for headers */
    }

    nav {
        padding: 10px; /* Reduced padding for navigation */
    }

    footer {
        font-size: 0.9em; /* Smaller footer text */
    }
}

footer {
    text-align: center;
    color: rgb(255, 255, 255);
    width: 100%;
    bottom: 0;
    position: fixed;
    padding: 12px;
    background-color: #c0392b; /* Matching footer color with header */
    box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.2); /* Subtle shadow for depth */
}
